"""Assignment 0 — Environment Setup (Onboarding).

This is the ONLY file you edit for this assignment.

Your tasks:
  1. Make `hello_course()` return the exact text: Hello, Algorithms!
  2. After your tests pass, change `isCompleted` below to True.

Run the tests from the Codespaces **Testing** panel (the flask icon) — you do
not need a terminal. See INSTRUCTIONS.md for the full step-by-step guide.
"""

# When you have finished AND all tests pass, set this to True.
# The website Dashboard unlocks this assignment's card when it sees `True`.
isCompleted = False


def hello_course() -> str:
    """Return a friendly greeting for the course.

    The tests expect this function to return the exact string:

        Hello, Algorithms!

    Replace the line below with a `return` statement that hands back that
    exact text (including the comma and the exclamation mark).
    """
    # TODO: return the exact string "Hello, Algorithms!"
    raise NotImplementedError("Complete hello_course() in solution.py")
