"""Assignment 8 — Sorting Efficiency.

This is the ONLY file you edit. Implement the sorts, run the tests, then set
isCompleted = True at the bottom.
"""

# Flip to True after all tests pass (unlocks the Dashboard card).
isCompleted = False


def bubble_sort(values: list) -> list:
    """Return a NEW sorted list using bubble sort (ascending).

    Do not use Python's built-in sorted()/list.sort(). Work on a copy so the
    caller's list is not changed.
    """
    # TODO: repeatedly swap adjacent out-of-order pairs until sorted
    raise NotImplementedError("Implement bubble_sort")


def insertion_sort(values: list) -> list:
    """Return a NEW sorted list using insertion sort (ascending).

    Do not use Python's built-in sorted()/list.sort().
    """
    # TODO: grow a sorted prefix by inserting each item into place
    raise NotImplementedError("Implement insertion_sort")
